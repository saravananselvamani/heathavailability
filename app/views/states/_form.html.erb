<%= form_for @state do |f| %>
    <% if @state.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@state.errors.count, "error") %> prohibited this state from being saved:</h2>

          <ul>
            <% @state.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div>
      <%= f.label "Doctor Count" %>
      <%= f.text_field :doctor_count %>
    </div>

    <div>
      <%= f.label "Nurse Count" %>
      <%= f.text_field :nurse_count %>
    </div>

    <div class="actions">
      <%= f.submit "Submit" %>
    </div>
<% end %>
